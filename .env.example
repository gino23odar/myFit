# Optional local database values used by docker compose.
# Spring Boot uses the same defaults from backend/src/main/resources/application.yml.
POSTGRES_DB=wardrove
POSTGRES_USER=wardrove
POSTGRES_PASSWORD=wardrove
